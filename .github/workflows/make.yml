# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: make

permissions:
  contents: read

on:
  push:
    branches: [main]
    tags: [v*]
  pull_request:

jobs:
  make:
    # TEMPORARY bridge to metanorma/ci#397 (single root Gemfile/Makefile
    # shape): the @v1 reusable still expects per-flavor Gemfiles, which this
    # PR removes. Revert to @v1 once ci#397 merges and the tag advances.
    uses: metanorma/ci/.github/workflows/standoc-models-make.yml@fix/model-template-final-lml
